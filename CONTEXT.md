# 回声追猎 / Echo Chase

本文件是项目的玩法词汇表和边界。它取代旧的《迟相 / Phase Lag》双空间协作设定。

## 核心命题

延迟不是输入卡顿。玩家正在走的路径会在 `N` 秒后化成过去体，并回头追上现在；玩家把一段已经发生的路径回传后，又会短暂形成未来可能性。关卡的张力来自同一条路径在不同时间的用途，而不是额外规则拼接。

## 时态实体

**现在体 / EchoPlayer**

玩家正在直接操控的 `CharacterBody2D`。它受地形、陷阱和输入影响，记录每个 physics tick 的可见状态。过去体接触现在体即失败。

**过去体 / PastEcho**

永久存在的 `Area2D`。它读取现在体在 `t - N` 的路径，不重跑输入，不受地形或陷阱影响。它可压压力板、挡光和触发只要求“存在”的机关；它不能拾取、攻击或主动操作。切档的 `0.6s` 相位期内完全不碰撞、不伤害、不触发机关。

**未来可能性 / FutureEcho**

由一次已提交录像生成的短暂 `Area2D`。它逐帧播放那段录像，忽略地形和陷阱；播放到录像终点后立即失去机关作用、归还可能性槽，再播放短暂消散尾帧。最多两个未来体可同时影响世界。

**实时轨迹 / TemporalTrack**

记录 `TemporalFrame` 的有序路径。它保存位置、速度、朝向、动画名与离散动作标志。普通段可插值；回传使用同一时刻的“普通末帧 + 回传帧”断点，绝不在传送段插值。

**回传 / Recall**

玩家在记录中按 `L` 结束录像，立即回到记录器起点。回传写入现在体历史，所以过去体在延迟后也会重演这次瞬移。现在体与刚生成的未来体获得 `0.35s` 相位期，避免回传瞬间自碰撞。

**过去延迟 / Past Delay**

过去体读取的时间偏移 `N`。当前合法值为 `1s`、`3s`、`5s`，默认 `3s`。触碰 `DelayPickup` 后先进入 `0.6s` 非交互相位期，再切换到新值。它持续生效，直到另一延迟拾取物或显式时间线重置。

**记录器 / FutureRecorder**

玩家进入时自动开始录像，并立即占用一个可能性槽。最短录像 `1s`、最长 `5s`；到达上限自动提交。录像结束后玩家必须离开该记录器区域才能再次触发它。

**时间优先级**

| 来源 | 现在体 | 过去体 | 未来体 |
| --- | --- | --- | --- |
| 过去体 | 失败 | 无效 | 消散 |
| 现在体 | 无效 | 无效 | 消散 |
| 未来体 | 无效 | 无效 | 穿透 |

## 可用机关语法

- 过去体和未来体能提供重量：压力板、挡光、存在型开关。
- 过去体只会晚到，不能替玩家处理玩家未处理过的危险。
- 未来体只会重演已录路径，不能临时响应玩家之后的输入。
- 机关必须把“同一路径在晚些时候再次出现”转化为选择或时机，不应要求玩家心算 physics tick。
- 陷阱只威胁现在体；过去体对陷阱免疫不等于它能替玩家清理陷阱。

## 检查点与重置

检查点只保存 authored 场景路径、检查点 ID 和已经稳定抵达的设备状态。不保存现在体血量、在途录像、过去延迟切档过程、未来体、敌人临时状态或任何半完成时间线。恢复或失败时先复位玩家到稳定出生点，再调用 `EchoTimelineController.reset_timeline()`。

## 作者边界

- 用户负责灰盒、路线、关卡空间、机关位置、出口、角色成稿与最终试玩删改。
- 代码只提供可实例化的玩家、时间体、记录器、延迟拾取物、压力板和门；不动态生成关卡替代品。
- 在两个用户灰盒通过试玩前，不扩章节、Boss、叙事系统、正式美术或地图内容。

## 明确排除

- 不保留旧的双角色、双空间、因果队列、电路墙板、相位猎手和五章结构。
- 不做在线联机、任意规则编辑、输入重演模拟、程序化地图、第二个追杀者或正式关卡生成。
- 不迁移旧开发存档；当前 Echo Chase 存档 schema 是新的最小格式。
