# 延迟追迹 / Delay Trace

本文件是项目的玩法词汇表和边界。它取代旧的《迟相 / Phase Lag》双空间协作设定。内部资源路径、类名和存档键仍以 `EchoChase` 为开发代号。

## 核心命题

延迟不是输入卡顿。玩家正在走的路径会在 `N` 秒后化成过去体，并回头追上现在；玩家把一段已经发生的路径回传后，又会短暂形成未来可能性。关卡的张力来自同一条路径在不同时间的用途，而不是额外规则拼接。

## 时态实体

**现在体 / EchoPlayer**

玩家正在直接操控的 `CharacterBody2D`。它受地形、陷阱和输入影响，记录每个 physics tick 的可见状态。普通状态下接触过去体即失败；未来录像中接触过去体则立即提交录像并回传。

**过去体 / PastEcho**

永久存在的 `Area2D`。它读取现在体在 `t - N` 的路径，不重跑输入，不受地形或陷阱影响。它可压压力板、挡光和触发只要求“存在”的机关；它不能拾取、攻击或主动操作。切档的 `0.6s` 相位期内完全不碰撞、不伤害、不触发机关。

**未来可能性 / FutureEcho**

由一次已提交录像生成的短暂 `Area2D`。它逐帧播放那段录像，忽略地形和陷阱；播放到录像终点后立即失去机关作用、归还所属记录器，再播放短暂消散尾帧。每台 authored 记录器独占一个未来体，因此并存上限等于场景中的记录器数量。

**实时轨迹 / TemporalTrack**

记录 `TemporalFrame` 的有序路径。它保存位置、速度、朝向、动画名与离散动作标志。普通段可插值；回传使用同一时刻的“普通末帧 + 回传帧”断点，绝不在传送段插值。

**回传 / Recall**

玩家在记录中按 `L` 结束录像。`L` 可在录制第一帧提交；不足 `1s` 的轨迹保持末帧补足到 `1s`。提交后世界恢复到开始录像时的时间锚点，只有新未来体保留刚录下的路径；现在体与新未来体获得 `0.35s` 相位期，避免回退瞬间自碰撞。现在体撞上未来体时，未来体照常消散，并恢复唯一一格冲刺；已有冲刺时不叠加。

**时间锚点 / Temporal Anchor**

开始录像时捕获现在体运动、过去体状态、延迟选择和所有已有未来体的轨迹与精确播放进度。提交后恢复这些状态，再从第 `0s` 启动新未来体。它只覆盖当前已有的时态玩法状态，不是通用世界序列化系统。

**过去延迟 / Past Delay**

过去体读取的时间偏移 `N`。当前合法值为 `1s`、`3s`、`5s`，默认 `3s`。`DelayPickup` 是保留旧类名的可重复延迟台，不会在触碰后消失。触碰新台后先进入 `0.6s` 非交互相位期，再切换到新值；预警中连续触碰只覆盖最新目标，不重置剩余时间。每个 authored 场景必须有唯一默认 `3s` 台和唯一 `delay_switch_id`。

**记录器 / FutureRecorder**

每台记录器 authored 一个独占 `FutureEcho`。玩家进入空闲记录器时自动开始录像；全局同时只能录一条，录像可立即提交，最长 `5s`。记录器在自身未来体存在时保持占用，未来体释放且玩家离开后才能再次触发。

**时间优先级**

| 来源 | 现在体 | 过去体 | 未来体 |
| --- | --- | --- | --- |
| 过去体 | 失败 | 无效 | 消散 |
| 现在体 | 无效 | 无效 | 消散 |
| 未来体 | 无效 | 无效 | 穿透 |

## 可用机关语法

- 过去体和未来体能提供重量：压力板、挡光、存在型开关。
- 过去体只会晚到，不能替玩家处理玩家未处理过的危险。
- 未来体只会重演已录路径，不能临时响应玩家之后的输入。
- 机关必须把“同一路径在晚些时候再次出现”转化为选择或时机，不应要求玩家心算 physics tick。
- 陷阱只威胁现在体；过去体对陷阱免疫不等于它能替玩家清理陷阱。

## 检查点与重置

检查点只保存 authored 场景路径、检查点 ID、复活坐标、玩家选中的 `past_delay_seconds` 和精确 `delay_switch_id`。预警中的最新选择也视为当前选择，但不保存剩余预警时间。不保存现在体血量、在途录像、未来体、敌人临时状态或任何半完成时间线。恢复或失败时先复位玩家到稳定坐标，再用检查点延迟参数调用 `EchoTimelineController.reset_timeline()`。

## 剧情闭环

无名男性主角因恋人 Emilia（昵称 Lia）牺牲而产生幸存者罪疚。他误以为自己的逃跑害死了 Lia，于是主动进入中立的时间裂隙，请求拿走过去和未来。裂隙把同一个人拆成追来的 Past、逃跑的 Present、未兑现的 Future，以及只剩现在的 Keeper。母题固定为 `RUN. 我会追上你。`

四枚 `MemoryShard` 使用稳定 ID：Hub 前的 `memory_after`、`1s` 支路的 `memory_t_minus_1`、`3s` 支路的 `memory_t_minus_3`、`5s` 支路的 `memory_t_minus_5`。每次拾取暂停玩法与倒计时，在黑屏中以红字逐句播放可独立理解的短闪回，不使用普通对话气泡；四枚集齐后，终点回头路线显现并按 `-5s → -3s → -1s → 事后` 重组完整记忆。碎片与支路顺序自由，拾取后立即写入当前槽位。

`30:00` 是 Keeper 为催促玩家编造的假期限。归零不失败、不清档，只通过 `NarrativeSlotModule` 记录并播放一次坦白。三支路完成后普通出口始终可选；普通结局先让 Keeper 误以为交接成功，再由裂隙揭示四个时态身份属于同一人并洗去新旧两人的记忆，Continue 返回终点岔路。四碎片解锁的回头路线包含真结局专属知识锁：玩家在录像中主动让 Past 追上自己；合并后的主角以金字和裂隙交流，令亲手记录的 Future Trace 回到原本的 `RUN` 时刻，最终与 Lia 一起离开。

`Scenes/EchoChase/echo_chase_start.tscn` 是当前可试玩 Jam 原型基线。新游戏先播放按当前语言选择的黑屏红字 `跑/RUN`；玩家接近 authored 世界位置后，以手写飘字显示当前跳跃与方向冲刺键位，Continue 不重复。它保留连续 19 房 authored 地图、三条自由顺序支路和原有时态机关，并接入清晰放大的世界内倒计时、四枚记忆碎片、普通出口、真结局回头路线、知识锁、时态粒子和双结局演出。场景布局仍由作者最终试玩和删改。

## 作者边界

- 用户负责灰盒、路线、关卡空间、机关位置、出口、角色成稿与最终试玩删改。
- 代码提供可实例化的时态机关、记忆闪回、知识锁和结局 presenter；所有世界节点、碰撞、布局与视觉都 authored 在 `.tscn` 中，不动态生成关卡替代品。
- 起始场景负责当前 Jam 原型的完整可玩闭环；后续仍直接在此场景上迭代，不另造第二套地图。
- 不扩章节、Boss、新叙事框架、正式美术或地图生成系统。

## 明确排除

- 不保留旧的双角色、双空间、因果队列、电路墙板、相位猎手和五章结构。
- 不做在线联机、任意规则编辑、输入重演模拟、程序化地图、第二个追杀者或正式关卡生成。
- 不迁移旧开发存档；当前 Echo Chase 存档 schema 是新的最小格式。
